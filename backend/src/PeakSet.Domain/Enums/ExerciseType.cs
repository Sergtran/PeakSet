namespace PeakSet.Domain.Enums;

/// <summary>
/// Exercise type as handled by the app (standard, bw, weighted, time).
/// </summary>
public enum ExerciseType
{
	Standard = 0,
	Bodyweight = 1,
	Weighted = 2,
	Time = 3
}
